<Window x:Class="CarSearch.Wishlist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarSearch"
        mc:Ignorable="d"
        Title="Wishlist" Height="450" Width="800" Background="ForestGreen">
    <StackPanel>
        <TabControl x:Name="TabCollection" SelectedItem="{Binding SelectedTab, Mode=OneWayToSource}">
            <TabItem Header="Cars">
                <local:WishlistDisplay/>
            </TabItem>
        </TabControl>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Height="25" Width="150" Content="Add To My Wishlist" Margin="5" Command="{Binding AddToWishlistCommand}" 
                    CommandParameter="{Binding ElementName=TabCollection, Path=Items}"/>
            <Button Height="25" Width="150" Content="Show Me My Wishlist" Margin="5" Command="{Binding OpenCartCommand}"/>
        </StackPanel>
        <Border BorderBrush="Gold" BorderThickness="5" Width="785" Margin="4,0,4.333,0"/>

    </StackPanel>
    
</Window>
